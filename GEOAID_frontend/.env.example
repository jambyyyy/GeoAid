# Optional: override the Django API URL when frontend and backend are hosted separately.
# VITE_API_URL=http://127.0.0.1:8000

# Optional: change the Django development port without editing source code.
# VITE_API_PORT=8000
